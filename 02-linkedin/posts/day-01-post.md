Day 1 of my DevOps learning journey

Today i understood the Linux filesystem-learning the actual purpose of directories like `/etc`, `/var`, `/proc`, and `/dev`, rather than just memorizing their names

I practiced basic commands (`ls`, `mkdir`, `cp`, `mv`, `rm`) and experimented with globbing patterns-things like `*.log`.

I also made a mistake i got confused between creating a file and a directory using `mkdir` that got cleared up after trying it out myself in the terminal.

Tomorrow, I’ll start with Text processing

#DevOps #Linux #LearningInPublic
