# Day 3 wrap-up 

spent 2 days trying to learn usermod, groupadd, gpasswd, groupdel
it just wasnt going into my head no matter how many times i tried
commands dont stay with me if there's no real problem behind them thats what i realized.

so i stopped forcing it what i already know well from day 3:
- UID/GID and how they work
- primary vs supplementary groups
- whoami, id, groups
- /etc/passwd, /etc/group, /etc/shadow basics
- useradd, passwd, userdel
- basic idea of service users

the rest (usermod, groupadd, gpasswd, groupdel, /etc/skel in depth) leaving this for later, for when i actually face a real situation that needs it. 
it will make more sense then than just reading commands now.

took me 2 days to accept this, but moving to the next topic now
